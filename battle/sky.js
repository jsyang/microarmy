// Generate sky ////////////////////////////////////////////////////////////////////////////////////////////////////////
// Either randomly by default or via time of day / weather conditions

SkyGenerator = Class.extend({
  init:function(params){
    this._=$.extend({
      
    },params);
  }
});
