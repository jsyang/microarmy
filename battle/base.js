// Generate starting bases /////////////////////////////////////////////////////////////////////////////////////////////
// Either randomly by default or via tile information

BaseGenerator = Class.extend({
  init:function(params){
    this._=$.extend({
      
    },params);
  }
});
